# PR Summary Generator

A web form that generates a condensed summary of open pull requests for a repository. The summary is written to clipboard as HTML and can be pasted into Slack or any other rich text input which supports HTML rich text. There are no dependencies.
